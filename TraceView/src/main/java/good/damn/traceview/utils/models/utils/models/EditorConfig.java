package good.damn.traceview.utils.models.utils.models;

import good.damn.traceview.utils.models.graphics.editor.EntityEditor;

public class EditorConfig extends EntityConfig {
    public EntityEditor entityEditor;

    public EditorConfig(float fx, float fy, float tx, float ty) {
        super(fx, fy, tx, ty);
    }
}
